import './globals.css'; // ✅ This is needed!

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}


// links import 
// add content to all parts
// left per sign in shit add and connect it with database create login page that works with all of thatvide
// texts import 
//add videos a loading screen  